<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.navdrawer">

    <dist:module dist:instant="true" />

    <!--TODO PERMIT INTERNET USES-->
    <uses-permission android:name="android.permission.INTERNET" /> <!--To Let User Using Internet-->
    <application
        android:allowBackup="true"
        android:icon="@drawable/logos"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.Login">

        </activity>

        <activity android:name=".activity.SplashScreen"
            android:theme="@style/AppTheme.NoActionBar">   <!--So it's start from SplashScreen-->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
    </application>

</manifest>